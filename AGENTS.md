# 项目协作说明

开始处理本项目的任何任务前，必须完整阅读根目录的
[`PROJECT_MEMORY.md`](./PROJECT_MEMORY.md)。它是本项目跨对话共享的长期记忆，
包含产品目标、架构、启动方式、业务规则、当前版本和已知限制。

## 记忆维护

完成以下任一类关键改动时，必须在同一次任务中同步更新
`PROJECT_MEMORY.md`，不要等待用户再次提醒：

- 产品目标、核心工作流或页面入口变化；
- 抓取器、标题生成器、竞品筛选、ABA 词库或评论痛点规则变化；
- 本地启动方式、端口、版本校验标识或部署地址变化；
- 数据模型、主要接口、关键文件职责或运行依赖变化；
- Sites 访问策略、部署方式或其他会影响后续工作的限制变化；
- 修复了一个容易复发、后续开发必须知道的重要问题。

普通样式微调、文案修正、一次性排查过程和临时日志不写入长期记忆。
更新记忆时只记录已经实现并验证的现状；过时内容应直接替换，不要不断追加
互相冲突的历史说明。涉及版本时，同时同步后端、前端和启动脚本的版本标识。

## 工作约束

- 用户现有改动与未跟踪文件不得擅自删除或覆盖。
- 本地真实抓取器依赖用户电脑上的 Chrome 和 Amazon 页面，不能用模拟数据冒充。
- 商品事实、标题卖点和评论改进必须有本品资料支持；竞品差评只能形成候选痛点，
  不能直接变成本品声明。
- 类目、尺寸单位和场景证据不足时提示人工确认，不得回退到固定的 Area Rug 规则。
- 完成代码变更后，按风险运行 Python 测试与 `npm test`；版本切换还要检查
  `http://127.0.0.1:8765/health`。

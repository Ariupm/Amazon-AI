# 采数 Amazon 标题半自动化工作台——项目记忆

最后更新：2026-08-02
当前本机版本：V26 / API `1.26.0`
特征标识：`category-aware-size-scenes-v26`

## 1. 项目目标

这是面向 Amazon 运营的真实数据标题工作台，不是演示页面。核心目标是：

1. 读取现有父体或单个子体的真实 Amazon 商品资料；
2. 先确认本品事实、竞品定义、搜索词及权重，再搜索竞品；
3. 综合头部竞品标题结构与词频、ABA 搜索词及流量排名、否词库、评论痛点和
   本品已验证改进；
4. 为不同尺寸分别整理词和场景，再生成自然、可读、兼顾流量与点击率的标题；
5. 支持主标题 + Highlight Item 二段标题以及多尺寸 Excel 导出。

原则：流量服从相关性和事实准确性。不得为了流量使用错误类目、错误尺寸场景、
未经证实的功能或竞品品牌词。

## 2. 主要入口与启动方式

- GitHub 主仓库：`https://github.com/Ariupm/Amazon-AI`，默认分支 `main`。
- 新电脑先双击 `安装项目依赖.bat`；该脚本通过 `setup.ps1` 检查 Node.js 22.13+、
  Python 3.10+ 和 Google Chrome，并安装 npm/Python 依赖。
- 本地工作台：`http://localhost:3000/titles`
- 若 3000 被其他项目占用，启动器会改用 3001。
- 本地抓取 API：`http://127.0.0.1:8765`
- 用户日常只需双击根目录的 `启动真实抓取器.bat`。
- 服务窗口必须保持开启；关闭服务窗口会停止前端、API 和专用 Chrome。
- `run_scraper.ps1` 会核对后端特征标识，避免旧抓取器占用 8765。

本机抓取器使用 Playwright 驱动本机 Google Chrome，并持久化
`amazon_scraper/.chrome-profile` 登录状态。V25 起 Chrome 在抓取器服务生命周期内
常驻复用：单次任务结束不会关闭浏览器；只有服务停止、浏览器被用户关闭，或切换
站点/可见模式需要重建会话时才会退出。Amazon 空白页或临时导航失败会自动重试
最多 3 次；验证码或登录需要用户在可见 Chrome 中完成。

## 3. 核心代码位置

- `app/titles/market-workspace.tsx`：标题工作台主 UI、数据流、版本校验、前端事实提取。
- `amazon_scraper/app.py`：FastAPI 接口、串行任务锁、常驻浏览器会话。
- `amazon_scraper/scraper.py`：商品/父子体/评论真实抓取、Chrome 生命周期、导航重试。
- `amazon_scraper/competitors.py`：竞品搜索方案、Amazon 搜索页抓取、相似度和去重。
- `amazon_scraper/title_generator.py`：ABA 选词、竞品词频与结构、尺寸场景、标题生成。
- `amazon_scraper/keyword_files.py`：ABA 与否词文件识别。
- `amazon_scraper/excel_export.py`：商品、竞品和多尺寸标题 XLSX 导出。
- `amazon_scraper/models.py`：前后端请求响应模型。
- `tests/test_title_generator.py`：标题与类目/尺寸/语义去重规则测试。
- `tests/rendered-html.test.mjs`：页面、启动器和版本标识测试。

## 4. 当前真实工作流

### 商品范围

- “优化现有商品”支持父体批量优化、批量子体优化和单子体优化。
- 父体模式读取全部子体和尺寸，按公开月销量信号推荐主推子体；无法可靠判断时支持
  人工指定。
- 批量子体模式支持直接粘贴最多 100 个子体 ASIN（换行、空格或逗号分隔），批量
  读取后自动勾选成功匹配的子体，也允许继续取消或补选；每个子体独立使用自身标题、
  五点、颜色和尺寸生成，不会将颜色与尺寸交叉组合，同尺寸不同颜色也不会被合并。
- 单子体模式只修改指定商品，不连带生成整个父体。
- 新增变体读取父体，再输入新增颜色/尺寸。
- 全新商品没有 ASIN，必须人工提供类目、材质、风格、真实特征、尺寸及可选图片。

### 竞品发现

- 必须先生成并人工确认搜索方案，再访问 Amazon。
- 批量子体模式发现竞品时使用已选主子体 ASIN，不再依赖单值 ASIN 输入框；搜索
  操作区将翻页、可选详情复核和主搜索按钮集中展示，并在点击后立即显示搜索状态。
- 搜索词可启用/停用和人工赋权；权重决定各搜索组进入候选池的配额比例。
- 默认从 Amazon 搜索页获取；详情页复核为可选短名单步骤。
- 同一竞品品牌只保留一个代表商品。
- 先硬性排除错误产品类型、本品 ASIN 家族、自有品牌和人工否词，再计算相似度。
- 产品相似度权重：产品类型 35%、真实属性 30%、标题特征证据 15%、图片外观 20%。
- 销量、评价和价格只用于市场价值排序，不提高产品相似度。
- 候选上限支持扩充到 100，但最终数量可能因搜索页实际返回、去重和硬性排除而少于
  100。

### 标题研究与生成

- 标题关键词覆盖权重按用户要求为 40%。
- 先分析已锁定头部竞品的：
  - 高频词及覆盖率；
  - 标题长度与标点；
  - 信息槽位顺序和主导结构公式；
  - 不同尺寸的类目词、场景词和写法。
- ABA 词库与竞品词频是两个独立证据池，不能混为一谈。
- 优先选词库中流量较大且与本品事实、类目、尺寸、风格和场景一致的词；
  无合适词时才使用真实产品描述词。
- 高流量场景词有证据时可放主标题；`for` 可保留在完整流量短语中。
- 普通连接词如 `with`、`and` 尽量少用，但不能破坏必要语法。
- 主标题不是关键词清单：应参考竞品主导公式组织为自然信息块，逗号只用于分隔
  类目识别、核心差异点、规格与场景。
- 主标题和 Highlight 合计，同一语义卖点只出现一次。语义簇是可扩展规则，不是只
  写死 Non-Slip；例如 Non Slip / Non-Slip / Non Skid / Anti-Slip 属于同一簇。
- 风格词已由产品事实确认且有市场/ABA 支持时，应优先进入主标题。

### 评论痛点

- 评论痛点只从负向证据提取，需结合星级、否定/转折语境和问题词，不能把
  “looks nice”“good news”等好评片段当差评。
- 评论痛点用于发现消费者顾虑；只有用户确认本品确实完成对应改进，才能作为标题
  卖点候选。
- 痛点 UI 使用卡片、证据原句和“我方已改进”确认流程，避免关键词碎片堆叠。

## 5. 类目、尺寸与场景规则

V26 已移除标题生成阶段的固定 Area Rug 回退：

- 类目以人工确认字段为准；标题自动识别只作回填建议。
- 已明确支持从标题识别 Dish Drying Mat、Coffee Mat、Bath Mat、Door Mat 及 Rug
  细分类目；其他类目识别不到时必须人工填写，不能默认为 Area Rug。
- ABA 词按当前类目的核心词重合度过滤。高流量但属于其他类目的词必须排除。
- 无单位尺寸按类目解释：
  - 非地毯类目如 Dish Drying Mat：`16x18` 解释为 16 × 18 英寸；
  - 地毯类目常见的裸数且边长不大于 12 时，按英尺惯例解释；
  - 支持英尺、英寸及类似 `1'6" x 1'8"` 的混合格式。
- 非地毯场景优先按同尺寸竞品标题、ABA 场景词频和人工主要用途排序。
  Dish Drying Mat 的可用证据场景包括 Kitchen Counter、Sink Side、Coffee Bar、
  RV Kitchen，但没有证据时不得强行写入标题。
- 地毯仍保留 Runner / Accent / Area Rug 的尺寸比例规则，但只在确认类目确实为
  rug/carpet 时启用。

## 6. 部署与访问现状

- Sites 项目配置位于 `.openai/hosting.json`，必须复用现有 `project_id`。
- 已有线上地址：`https://caishu-amazon-insights.chumoiii.chatgpt.site/titles`
- 线上页面仍依赖用户本机 8765 抓取器才能访问真实 Amazon 数据。
- 当前 Sites 访问模式已设为 public，任何持链接者均可在其他电脑打开网页。
- Sites 的 HTML 响应明确使用 `Cache-Control: no-store`；内容哈希静态资源仍可长期
  缓存。部署包需至少保留上一线上版本的哈希 CSS/JS，避免已打开页面在版本切换后
  请求旧资源时出现 404 和整页无样式。该问题曾在线上 V24 部署后实际发生。
- 线上页面的真实抓取接口仍指向访问者电脑的 `127.0.0.1:8765`；其他电脑若要执行
  Amazon 真实读取、竞品搜索等操作，仍需在该电脑运行本项目的本地抓取器。仅浏览
  页面不需要本地抓取器。
- 后续若访问策略再次变化，应先调用 Sites 权限工具核实再对外说明。

## 7. 验证与版本维护

常用验证：

```powershell
python -m unittest discover -s tests -p "test_*.py"
npm test -- --runInBand
Invoke-RestMethod http://127.0.0.1:8765/health
```

版本升级时必须同步：

1. `amazon_scraper/app.py` 的 API 版本与 `FEATURE_VERSION`；
2. `app/titles/market-workspace.tsx` 的 `REQUIRED_BACKEND`；
3. `run_scraper.ps1` 的 `$requiredFeatureVersion` 和控制台版本文案；
4. `tests/rendered-html.test.mjs` 中的版本断言；
5. 本文件顶部的当前版本和涉及的行为说明。

工作区常有用户未提交改动、日志、构建缓存和历史 Sites 压缩包。不得擅自删除、
重置或覆盖；只修改当前任务明确涉及的文件。

GitHub 提交必须排除 `.chrome-profile`、`.env*`、日志、`.pnpm-store`、构建缓存、
`*.tsbuildinfo` 和 `sites-version-*.tar.gz`；`public/assets` 中为线上旧哈希兼容而保留的
静态资源属于项目文件，可以提交。
